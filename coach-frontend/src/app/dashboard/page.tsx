import { Metadata } from "next";

export const metadata: Metadata = {
  title:
    "Dashboard | COA-CH",
  description: "This is Dashboard for COA-CH",
};

const Home: React.FC = () => {
  return (
    <>
      <div>Dashboard</div>
    </>
  );
}

export default Home;
